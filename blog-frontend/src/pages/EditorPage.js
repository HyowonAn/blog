import React from 'react';
import EditorTemplate from 'components/editor/EditorTemplate'

const EditorPage = () => {
    return (
        <EditorTemplate
            header="it's header"
            editor="it's editor"
            preview="it's preview"
        />
    );
};

export default EditorPage;